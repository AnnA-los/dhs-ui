# DHS UI Agent 工作准则

本文件是后续模型、会话、agent 接手 `dhs-ui` 时必须先读的协作约定。项目核心背景见 `docs/project`，UI 设计资料统一见 `docs/ui`。

## 文档沉淀规则

- `docs/project` 保存项目核心文档。它的目标是让新模型、新会话或新 agent 直接阅读该目录后，就能理解项目架构、核心业务、历史遗留问题、特殊处理和高风险改动点。
- 每一次需求完结后，都要补充需求总结，记录重点功能、踩过的坑、解决方法、特殊处理、临时方案、后续扩展限制和遗留问题。
- 文档按功能分类沉淀。比如权限管理是一类大功能，就维护权限管理文档；如果后续只是优化权限管理或给它加功能，应追加到权限管理文档中，不要另起新文档。
- `docs/project/pitfalls.md` 是前端高频错误总结文档。每次开发前都要先参考它，开发全过程都要持续对照执行；开发中再次遇到高频问题时，也要追加根因和解决方案，避免后续重复犯错。跨仓需求还必须同步参考 `dhs-core` 的 `docs/project/pitfalls-and-fixes.md`。

- 架构基线维护在 `docs/project/architecture-baseline.md`。后续新增功能、拆分模块、调整模块职责时，必须同步更新该文档，写清模块边界、相关页面/API、关键风险和扩展注意。

## UI 设计规则

- 所有 UI 设计方案、视觉说明、交互稿、截图验收记录等，都放到 `docs/ui`。
- 如果需求涉及前端页面开发，`dhs-core` 的设计文档可能已经给出 UI 设计图或页面说明；`dhs-ui` 侧只维护一份 UI 设计图生成/说明文档，放在 `docs/ui`，并在文档中互链对应的 `dhs-core` 设计文档。不要在 core 和 ui 两边各自维护一套重复 UI 设计图。
- UI 开发可以借用 `ui-ux-pro-max` skill 的能力，但必须符合本系统现有整体风格：Vue2 + Element UI 管理后台、医疗业务、克制的工作台气质。
- 不要为了单个页面引入明显割裂的视觉体系。新增页面优先延续现有布局、表格、弹窗、表单、颜色、间距和按钮习惯。

## 复用优先

- 可复用的组件、弹窗、表单片段，优先抽成公共组件或公共弹窗。
- 开发前先搜索现有组件和同类页面实现，优先复用 `src/components`、`src/views/medical/components`、已有 mixin、全局工具方法和同类业务模式。
- 不要把同一类弹窗、类型选择、远程下拉、上传、分页、表格高度计算逻辑在多个页面重复手写。

## 前后端协同

- 本项目是前后端分离系统：前端是 `dhs-ui`，后端是 `dhs-core`。
- 大多数需求会前后端同步开发。通常会在 `dhs-core` 中进行开发设计和测试用例设计，同时在 `dhs-ui` 中实现页面、交互和接口联调。
- 前端开发时要确认后端接口、菜单路由、权限标识、字典、数据状态码和返回结构；不要只从前端猜测业务规则。
- 业务菜单大多由后端 `/getRouters` 下发。新增或移动页面时，要同步确认后端菜单 `component` 路径。

## 子任务、自测、合并与联调流程

多会话并行开发时必须按以下顺序推进，不能只等所有子任务合并后再统一测试：

1. 子任务分支自测：每个子任务完成后，先在自己的子任务 worktree 内完成页面自测、接口字段核对、权限按钮核对和必要的静态检查；后端子任务需运行与改动直接相关的 Maven 测试。自测失败不得合回需求主分支。
2. core/ui 小联调：同一任务同时涉及 `dhs-core` 和 `dhs-ui` 时，必须在两个子任务都自测通过后做任务级联调，重点核对接口路径、请求字段、响应字段、枚举/字典、分页结构、权限标识、错误码和页面提示。
3. 合并到需求主分支：子任务合并回需求主分支前，必须确认没有 mock、占位接口、临时字段、临时菜单、临时权限或未替换配置残留；如果确实需要保留临时方案，必须写入任务文档和合并说明。
4. 需求主分支集成测：所有子任务合并后，在需求主分支执行完整相关测试。前端按项目约束不执行 `npm run build` 或 `npm run dev`，但必须做页面级检查和接口联调验证；后端至少运行本需求新增和受影响测试类，风险较大时运行 `mvn test`。
5. 真实联调测试：合并集成测通过后，使用本地或测试环境真实前后端服务走核心业务路径，覆盖登录注册、权限菜单、医院用户、角色管理、排班、邀请审核、短信配置、系统超管后台等跨模块链路。
6. 最终测试结论：需求结束时必须输出测试结论，至少包含每个子任务自测结果、core/ui 小联调结果、需求主分支集成测试命令和结果、真实联调测试结果、未测项、风险、回滚建议和是否建议进入上线/验收。

如果任一阶段发现问题，先回到对应子任务分支修复并补测试，再重新执行该阶段及其后续阶段。

## 需求分支与 worktree

- 从需求文档、开发设计或测试用例开始时，就要在对应仓库 worktree 新增需求主分支，文档也在该新分支下创建和维护。
- 严禁在主工作区直接创建需求主分支或子任务分支。
- 严禁在主工作区直接新增、修改、删除需求、设计、测试、任务拆分或功能代码文件。
- 主工作区只用于查看、诊断、创建 worktree 或做明确的用户指定操作；需求相关产物必须落在对应 worktree。
- 如果误在主工作区产生改动，必须先停止继续开发，将改动迁移到需求 worktree，并恢复主工作区干净后再继续。
- 开始需求文档、开发设计、测试用例或功能开发前，必须先确认当前路径是否位于需求 worktree；如果不是，先创建或切换到需求 worktree。
- 同一个需求内，`dhs-core` 和 `dhs-ui` 基本会协同开发；两边的需求主分支名称应保持一致，便于联调、追踪和最终合并。
- 需求主分支作为该需求最后汇总子任务分支的主分支。子任务完成后合并回需求主分支，再由需求主分支进入最终集成流程。
- 子任务分支按实际开发范围创建：如果某个子任务只需要改 `dhs-core`，`dhs-ui` 不需要创建该子任务分支；如果只需要改 `dhs-ui`，`dhs-core` 不需要创建该子任务分支。
- 用户可能只在 `dhs-core` 或只在 `dhs-ui` 发起需求说明/开发会话，但 agent 必须意识到这是双仓协同需求，主动检查是否需要同步另一侧的接口、菜单、权限、文档、UI 设计或测试约定。
- 在 `dhs-ui` 中涉及前端页面或接口联调时，要记录对应 `dhs-core` 分支/设计文档；反之亦然，保持两边分支和文档能互相追溯。

## 开发前必读

开始需求文档、开发设计、测试用例、任务拆分或功能开发前，必须先枚举并阅读或检索 `docs/project/` 下的所有文档文件，不能只阅读 README 或少量核心清单。

其中 `docs/project/pitfalls.md` 必须贯穿整个开发过程持续遵循。涉及 `dhs-core` 的跨仓需求，还必须枚举并阅读或检索后端 `docs/project/` 下的所有文档文件，尤其是 `docs/project/pitfalls-and-fixes.md`。

涉及 UI 设计时，同时阅读：

- `docs/ui/README.md`

## 完结检查

需求结束前确认：

- 是否已在需求主分支下完成文档和代码；如果涉及双仓，确认 `dhs-core`/`dhs-ui` 需求主分支名称一致或说明例外原因。
- 是否更新了对应功能文档或新增了必要的功能文档。
- 是否在新增/调整模块后同步更新了 `docs/project/architecture-baseline.md`。
- 是否把新踩坑追加到 `docs/project/pitfalls.md`。
- 是否把 UI 设计资料放入 `docs/ui`，并与对应 `dhs-core` 设计文档互链；确认没有在 core 和 ui 两边重复维护 UI 设计图。
- 是否复用了已有公共组件，或说明了没有抽公共组件的原因。
- 是否说明了验证结果；如果没有构建或没有联调，要明确写出。
